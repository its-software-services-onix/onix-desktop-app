<UserControl x:Class="Its.Onix.Ui.Client.Skeleton.Commons.UFormContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Its.Onix.Ui.Client.Skeleton.Commons"
             mc:Ignorable="d" 
             Width="{Binding FormWidth}"
             Height="{Binding FormHeight}"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
        <DockPanel LastChildFill="True">
            <Grid Height="30" DockPanel.Dock="Top" Margin="5,5,5,0" Background="Snow">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Style="{StaticResource ResourceKey=FormCaptionLabel}" Content="{Binding Caption}"/>
                <Image Grid.Column="1" Margin="2" Width="25" Height="25" Source="pos_cancel_void.png"/>
            </Grid>

            <DockPanel LastChildFill="True" DockPanel.Dock="Top" Margin="5" Background="WhiteSmoke">
            </DockPanel>
        </DockPanel>
    </Border>
</UserControl>
